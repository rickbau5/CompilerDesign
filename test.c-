int wallace, gromit;
char preston;
bool a, b;

int dog(bool corgi) {
    char cat;
    char frog[12];

    cat = 666;
    cat = dog;

    corgi = a and b;
    corgi = gromit and b;
    corgi = a and gromit;

    corgi = a == b;
    corgi = cat == cat;
    corgi = wallace == gromit;
    corgi = a == gromit;

    corgi = a != b;
    corgi = cat != cat;
    corgi = a != gromit;
    corgi = a != gromit;

    gromit = *frog;

    wallace = 14 * 1;

    return cat;
    return frog;
}
