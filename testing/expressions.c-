int main() {
    // Basic
    assign = ment;
    arr[1] = ay;
    ohne[2];
    structure.member = something;
    struct.x;
    struct.y.z = 'y';

    // Other assignment
    x += 1;
    y -= 2;
    z /= 4;
    v *= 6;
    v--;
    z++;

    // Mul ops
    four = 4 * 1;
    five = 15 / 3;
    one = 3 % 2;

    // Add ops
    two = 5 - 3;
    four = two + 2;

    // Unary ops
    neg = -1;
    mul = *6;
    question = ?4;
    multi = -*?yes;

    // Word ops
    ord = true or false;
    anded = false and (true or false);
    notted = not true;

    // Relative
    a == b;
    b != c;
    d <= e;
    f >= g;
    h < i;
    j > k;
}
